"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const teamsApi_1 = require("../../api/teamsApi");
const sinon = __importStar(require("sinon"));
describe('TeamApi Unit Test', () => {
    let teamApiStub;
    beforeEach(() => {
        teamApiStub = sinon.createStubInstance(teamsApi_1.TeamsApi);
        teamApiStub.createTeam = sinon.stub().resolves({ teamId: 'team_id' });
        teamApiStub.updateTeam = sinon.stub().resolves({
            success: true,
            message: 'Team updated successfully',
        });
    });
    afterEach(() => {
        sinon.restore();
    });
    it('should mock and verify createTeam call', () => __awaiter(void 0, void 0, void 0, function* () {
        const mockTeamCreated = {
            teamId: 'team_id',
        };
        const teamData = {
            name: 'New Team',
            description: 'A test team for Syncfusion',
        };
        teamApiStub.createTeam.resolves(mockTeamCreated);
        const result = yield teamApiStub.createTeam(teamData);
        expect(result).toBeDefined();
        expect(result.teamId).toBe('team_id');
        sinon.assert.calledOnceWithExactly(teamApiStub.createTeam, teamData);
    }));
    it('should mock and verify getTeamDetails call', () => __awaiter(void 0, void 0, void 0, function* () {
        const teamId = 'team_id';
        const mockTeamDetails = {
            teamId: 'team_id',
            name: 'Engineering Team',
            description: 'Handles product development',
            members: ['user1', 'user2'],
        };
        teamApiStub.getTeamDetails = sinon.stub().resolves(mockTeamDetails);
        const result = yield teamApiStub.getTeamDetails(teamId);
        expect(result).toBeDefined();
        expect(result.teamId).toBe('team_id');
        expect(result.name).toBe('Engineering Team');
        expect(result.members).toContain('user1');
        expect(result.members.length).toBe(2);
        sinon.assert.calledOnceWithExactly(teamApiStub.getTeamDetails, teamId);
    }));
    it('should mock and verify listTeams call', () => __awaiter(void 0, void 0, void 0, function* () {
        const mockListResponse = {
            teams: [
                {
                    teamId: 'team_1',
                    name: 'Engineering',
                    description: 'Handles product development',
                },
                {
                    teamId: 'team_2',
                    name: 'Marketing',
                    description: 'Manages marketing campaigns',
                },
            ],
        };
        teamApiStub.listTeams = sinon.stub().resolves(mockListResponse);
        const result = yield teamApiStub.listTeams();
        expect(result).toBeDefined();
        expect(Array.isArray(result.teams)).toBe(true);
        expect(result.teams.length).toBe(2);
        expect(result.teams[0].name).toBe('Engineering');
        expect(result.teams[1].teamId).toBe('team_2');
        sinon.assert.calledOnce(teamApiStub.listTeams);
    }));
});
//# sourceMappingURL=teamApi.spec.js.map