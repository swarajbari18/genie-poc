"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs = __importStar(require("fs"));
const api_1 = require("../api");
const documentApi = new api_1.DocumentApi();
documentApi.setApiKey("API_KEY");
const bounds = new api_1.Rectangle();
bounds.x = 100;
bounds.y = 50;
bounds.width = 100;
bounds.height = 100;
const formField = new api_1.EditFormField();
formField.fieldType = api_1.EditFormField.FieldTypeEnum.Signature;
formField.pageNumber = 1;
formField.editAction = api_1.EditFormField.EditActionEnum.Add;
formField.bounds = bounds;
var signers = new api_1.EditDocumentSigner();
signers.editAction = api_1.EditDocumentSigner.EditActionEnum.Add,
    signers.emailAddress = "signer1@gamil.com",
    signers.name = "signer1",
    signers.signerRole = "Signer";
signers.formFields = [formField];
var documentfile = new api_1.EditDocumentFile();
documentfile.file = fs.createReadStream("examples/documents/agreement.pdf");
documentfile.editAction = api_1.EditDocumentFile.EditActionEnum.Add;
var editDocumentRequest = new api_1.EditDocumentRequest();
editDocumentRequest.message = "Please review and sign the attached document.";
editDocumentRequest.signers = [signers];
editDocumentRequest.files = [documentfile];
var documentId = "DOCUMENT_ID";
function editDocument() {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            debugger;
            var editDocumentResponse = yield documentApi.editDocument(documentId, editDocumentRequest);
            console.log("Document edited successfully!", editDocumentResponse);
        }
        catch (error) {
            console.error("Error occurred while calling the API:", error.message);
        }
    });
}
editDocument();
//# sourceMappingURL=documentEdit.js.map